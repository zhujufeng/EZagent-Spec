{
  "name": "@ezagent/spec",
  "version": "0.4.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "files": [
    "dist/src/**/*.js",
    "!dist/src/experts/source-lock.js",
    "!dist/src/experts/importer.js",
    "!dist/src/experts/attested-source-contract.js",
    "README.md",
    "THIRD_PARTY_NOTICES.md",
    "LICENSE*",
    "licenses/**",
    "catalog/normalized/experts.json",
    "catalog/normalized/catalog.lock.json"
  ],
  "bin": {
    "ezagent": "./dist/src/cli/main.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "postbuild": "node scripts/set-cli-executable.mjs",
    "prepack": "npm run catalog:verify && npm run build",
    "prepublishOnly": "npm run catalog:verify && npm run build",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:core": "vitest run test/domain test/workspace test/cli",
    "test:experts": "vitest run test/experts",
    "test:codex": "vitest run test/codex",
    "test:hosts": "vitest run test/hosts",
    "test:workflow": "vitest run test/workflow test/e2e",
    "catalog:lock": "node --import tsx scripts/lock-catalog-sources.ts",
    "catalog:import": "node --import tsx scripts/import-experts.ts",
    "catalog:verify": "node --import tsx scripts/verify-catalog.ts",
    "generate:unicode-case-fold": "node --import tsx scripts/generate-unicode-case-fold.ts",
    "plugin:build": "node --import tsx scripts/build-plugin.ts",
    "plugin:check": "node --import tsx scripts/build-plugin.ts --check",
    "plugin:validate": "vitest run test/codex/plugin-manifest.test.ts test/codex/skill-contract.test.ts test/hosts/plugin-compatibility.test.ts",
    "plugin:verify": "npm run catalog:verify && npm run plugin:check && npm run plugin:validate && npm run test:codex",
    "plugin:host-eval": "node --import tsx scripts/codex-host-eval.ts run",
    "plugin:host-eval:verify": "node --import tsx scripts/codex-host-eval.ts verify",
    "plugin:post-init-eval": "node --import tsx scripts/codex-post-init-eval.ts run",
    "plugin:post-init-eval:verify": "node --import tsx scripts/codex-post-init-eval.ts verify",
    "verify": "npm run check && npm run test && npm run build"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "22.15.21",
    "esbuild": "^0.28.2",
    "execa": "^10.0.1",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "3.2.7"
  },
  "overrides": {
    "vite": "6.4.3"
  }
}
