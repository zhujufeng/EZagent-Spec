# EZagent Spec

EZagent Spec is an internal, local-only Spec Coding toolkit. The current package exposes the local core CLI used by the Codex plugin; it does not upload project data or perform Git operations automatically.

The `ezagent` command is an internal integration interface. Project initialization and later workflow automation are designed to be invoked through the plugin.
